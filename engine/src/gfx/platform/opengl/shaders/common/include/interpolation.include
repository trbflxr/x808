// Interpolation
float slerp(const float start, const float dest, const float delay) {
  const float start2 = max(start, 0.00000001);
  const float dest2 = max(dest, 0.00000001);
  return pow(dest2 / start2, delay) * start2;
}
